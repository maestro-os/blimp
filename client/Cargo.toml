[package]
name = "blimp"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
tokio = { version = "1.21.2", features = ["macros", "rt", "rt-multi-thread"] }

[features]
default = []
network = ["common/network"]
