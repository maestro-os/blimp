#!/bin/bash

set -e
cd *

mkdir build
cd build

../configure \
	--prefix="$SYSROOT/tools" \
	--target="$TARGET" \
	--with-sysroot="$SYSROOT" \
	--disable-werror \
	--disable-nls \
	--enable-64-bit-bfd \
	--enable-gprofng=no

make -j${JOBS}
make install -j1
